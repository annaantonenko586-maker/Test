# Тестовое задание — Аналитик данных
Антоненко Анна Георгиевна

---

## Блок 1: Теория вероятности и логика

### Задание 1: Фермер

**Ответ: 3,99**

Каждый заход независим, животное выбирается случайно из 6 видов. Введём индикаторы $X_i$ — был ли увиден $i$-й вид хотя бы раз за 6 заходов. Тогда общее число увиденных видов $X = X_1 + \ldots + X_6$.

По линейности математического ожидания:

$$E[X] = 6 \cdot P(\text{вид увиден}) = 6 \cdot \left(1 - \left(\frac{5}{6}\right)^6\right) \approx 3{,}99$$

---

### Задание 2: Кулинарное соревнование

**Ответ: 26,8**

Пронумеруем шефов от 1 (слабейший) до 80 (сильнейший). В любой паре побеждает тот, у кого номер выше.

Вероятность для шефа с рангом $k$ победить в одном раунде:
$$P_k = \frac{k-1}{79}$$

Раунды независимы, поэтому вероятность получить награду (победить в обоих):
$$P(\text{оба раунда}) = \left(\frac{k-1}{79}\right)^2$$

Математическое ожидание числа победителей:
$$E = \sum_{k=1}^{80} \left(\frac{k-1}{79}\right)^2 = \frac{1}{79^2} \cdot \frac{79 \cdot 80 \cdot 159}{6} \approx 26{,}8$$

---

### Задание 3: Одинокая дорога

**Ответ: 63,2; 93,3**

Поток автомобилей моделируется как пуассоновский процесс — появление за время $t$:
$$P(t) = 1 - e^{-\lambda t}$$

Из условия $P(30) = 0{,}95$ находим интенсивность:
$$\lambda = \frac{-\ln(0{,}05)}{30} \approx 0{,}0999 \text{ авт/мин}$$

За 10 минут: $P(10) = 1 - 0{,}05^{1/3} \approx 63{,}2\%$

За 27 минут: $P(27) = 1 - 0{,}05^{0{,}9} \approx 93{,}3\%$

---

## Блок 2: Python

### Задание 1: Изоморфизм строк

```python
def is_isomorphic(s: str, t: str) -> bool:
    if len(s) != len(t):
        return False
    s_to_t = {}
    t_to_s = {}
    for cs, ct in zip(s, t):
        if cs in s_to_t:
            if s_to_t[cs] != ct:
                return False
        else:
            if ct in t_to_s:
                return False
            s_to_t[cs] = ct
            t_to_s[ct] = cs
    return True

s = 'paper'
t = 'title'
print(is_isomorphic(s, t))  # True
```

Два словаря — маппинг s→t и обратный t→s. На каждом шаге проверяем, что замена согласована в обе стороны: это исключает ситуацию, когда два разных символа из s отображаются в один символ из t.

Время: O(n) — один проход. Память: O(k), где k — размер алфавита, фактически O(1).

---

### Задание 2: Пропущенное число

```python
def missing_number(nums: list) -> int:
    n = len(nums) + 1
    expected = n * (n + 1) // 2
    return expected - sum(nums)

nums = [1, 2, 3, 4, 5, 6, 8, 9, 10, 11]
print(missing_number(nums))  # 7
```

Сумма первых n натуральных чисел известна по формуле Гаусса. Разность между теоретической и фактической суммой — пропущенное число.

Время: O(n). Память: O(1) — никаких дополнительных структур.

---

### Задание 3: Факторизация

```python
def prime_factors(n: int) -> list:
    factors = []
    d = 2
    while d * d <= n:
        while n % d == 0:
            factors.append(d)
            n //= d
        d += 1
    if n > 1:
        factors.append(n)
    return factors

print(prime_factors(56))  # [2, 2, 2, 7]
```

Делим n на наименьший делитель начиная с 2 и проверяем делители только до √n — после каждого деления n уменьшается, что ускоряет работу.

Время: O(√n). Память: O(log n) — длина списка множителей.

---

## Блок 3: SQL

### Задание 1: Рейтинг абитуриентов

```sql
SELECT
    id,
    scores,
    RANK() OVER (ORDER BY scores DESC) AS position
FROM examination;
```

Использую `RANK()` — при равных баллах абитуриенты получают одинаковую позицию, следующая пропускается, что соответствует логике реального рейтинга. Если нужно без пропусков — `DENSE_RANK()`.

---

### Задание 2: FULL JOIN — диапазон строк

**Минимально 30 и максимально 600 строк**

Минимум: все 20 ключей второй таблицы совпадают с 20 из 30 ключей первой — получаем 20 совпавших пар + 10 строк без пары = 30 строк.

Максимум: если ключи не уникальны внутри таблиц — каждая из 30 строк первой соединяется с каждой из 20 строк второй = 600 строк.

При уникальных ключах внутри таблиц: минимум 30, максимум 50 строк.

---

### Задание 3: Клиенты с покупками менее 5000 руб.

```sql
SELECT a.client_id
FROM account a
JOIN transaction t ON t.account_id = a.id
WHERE t.type = 'buy'
  AND t.transaction_date >= DATE_TRUNC('month', CURRENT_DATE) - INTERVAL '1 month'
  AND t.transaction_date <  DATE_TRUNC('month', CURRENT_DATE)
GROUP BY a.client_id
HAVING SUM(t.amount) < 5000;
```

JOIN связывает счета с транзакциями, фильтрую только покупки за последний полный месяц, группирую по клиенту (агрегируя все его счета), HAVING отсекает тех, кто потратил 5000 и более. Подзапросы и оконные функции не использованы.

---

## Блок 4: Статистика и A/B-тесты

### Задание 1: Самокат

Верные утверждения: **3 и 4**

Тест проводился в одном небольшом городе — это и есть его генеральная совокупность. Распространять результаты на крупные города некорректно: другая инфраструктура, плотность, маршруты. При этом сам по себе эффект подтверждён, и это достаточная причина расширить эксперимент на другие небольшие города, прежде чем масштабировать на всю страну.

---

### Задание 2: p-value

Верный ответ: **вероятность наблюдения такого или более экстремального результата при условии, что нулевая гипотеза верна.**

p-value — это не вероятность того, что нулевая гипотеза верна, и не уровень значимости. Это вероятность получить данные настолько же (или более) отличающиеся от H0, если бы H0 была верна.

---

### Задание 3: t-критерий при логнормальном распределении

**Да, можно.**

При выборке более 5 млн наблюдений центральная предельная теорема работает уверенно: выборочное среднее распределено приближённо нормально вне зависимости от исходного распределения. Выбросы при этом отсутствуют, целевая метрика — среднее. Условия для t-критерия выполнены.

---

## Блок 5: ML Base

### Задание 1: Пони тоже кони

Возьму модель с ROC-AUC = 0,1 и инвертирую предсказания.

ROC-AUC = 0,5 — это случайный классификатор. Модель с ROC-AUC = 0,1 систематически ошибается, то есть она не случайна — она просто предсказывает наоборот. Если проинвертировать её предсказания, получим ROC-AUC = 0,9. Модель с 0,7 тоже рабочая и хороша как baseline, но 0,9 после инверсии — очевидно лучше.

---

### Задание 2: ROC-AUC вручную

**Ответ: 0,75**

Всего: P = 7 положительных, N = 8 отрицательных.

Использую метод U-статистики Манна-Уитни:
$$\text{AUC} = \frac{\text{concordant pairs}}{P \times N}$$

Конкордантная пара — (pos, neg), где score_pos > score_neg. Считаю для каждого положительного объекта, сколько отрицательных имеют меньший score:

| Pos score | Конкордантных пар |
|:-:|:-:|
| 0,95 | 8 |
| 0,85 | 7 |
| 0,75 | 6 |
| 0,70 | 6 |
| 0,65 | 6 |
| 0,60 | 6 |
| 0,40 | 3 |

Итого: 8 + 7 + 6 + 6 + 6 + 6 + 3 = 42

$$\text{AUC} = \frac{42}{7 \times 8} = \frac{42}{56} = 0{,}75$$

---

### Задание 3: Корреляция Пирсона

**Ответ: −0,85**

| Кофе (x) | Балл (y) |
|:-:|:-:|
| 1 | 85 |
| 1 | 88 |
| 2 | 79 |
| 2 | 81 |
| 2 | 84 |
| 2 | 65 |
| 3 | 67 |
| 3 | 58 |
| 3 | 76 |
| 4 | 49 |

$n = 10$, $\bar{x} = 2{,}3$, $\bar{y} = 73{,}2$

$$r = \frac{\sum(x_i - \bar{x})(y_i - \bar{y})}{\sqrt{\sum(x_i - \bar{x})^2 \cdot \sum(y_i - \bar{y})^2}} = \frac{-83{,}4}{\sqrt{5{,}1 \times 1604{,}4}} \approx -0{,}85$$

Корреляция сильная отрицательная. Однако это не означает причинно-следственной связи — скорее всего оба показателя обусловлены третьим фактором (например, уровнем подготовки или тревожности). Студенты, которые меньше готовились, пили больше кофе перед экзаменом и в итоге получали меньше баллов. Для установления причинности нужен контролируемый эксперимент.
